class Solution {
    public int lengthOfLongestSubstring(String s) {
        int ans = 0; // storing legth of longest contiguous string
        int i = 0; // start pointer 
        int j = 0; // end pointer
        int n = s.length()
        while (i < n && j < n) {
            
        }
        
    }   
    public boolean isUniqueChars(String s, int start, int end){
        Set<Character> letters = new HashSet<>();
        for (int i = start, i < end, i++) {
            Character ch = s.charAt(i);
            
        }
    }
}